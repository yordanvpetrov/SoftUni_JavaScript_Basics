function projectsCreating(name, qtyProjects) {
    let neededHours = qtyProjects * 3;
    console.log(`The architect ${name} will need ${neededHours} hours to complete ${qtyProjects} project/s.`);
    
}