function petShot(qtyDogFood, qtyCatFood) {
    let dogFoodPrice = 2.50;
    let catFoodPrice = 4;
    let totalPrice = qtyDogFood * dogFoodPrice + qtyCatFood * catFoodPrice;
    console.log(`${totalPrice}`);
    
}